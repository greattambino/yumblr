(function(root) {
  'use strict';

  var RestaurantConstants = root.RestaurantConstants = {
    RESTAURANTS_RECEIVED: "RESTAURANTS_RECEIVED",
    RESTAURANT_RECEIVED: "RESTAURANT_RECEIVED"
  };
}(this));
